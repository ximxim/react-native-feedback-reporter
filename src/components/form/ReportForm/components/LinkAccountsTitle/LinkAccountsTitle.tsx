import React, { FunctionComponent } from 'react';

import { Typography } from '../../../../ui';

export const LinkAccountsTitle: FunctionComponent<unknown> = () => {
  return (
    <Typography variant="h1" textAlign="center" my="30">
      Link Account(s)
    </Typography>
  );
};
